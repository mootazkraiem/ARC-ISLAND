name: Build unsigned iOS .ipa (for SideStore)

on:
  workflow_dispatch: {}
  push:
    branches: [main, master]

jobs:
  build-unsigned-ipa:
    runs-on: macos-26
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

<<<<<<< HEAD
      # Pinned, not left on the runner's default: Expo SDK 57's
      # expo-modules-jsi has two conflicting requirements — its own Swift
      # Package needs Swift tools >= 6.2 to resolve, but its C++/Swift
      # interop header breaks specifically on Swift 6.2.0–6.2.3 (Xcode
      # 26.0–26.2). Swift 6.3 (Xcode 26.5+) satisfies both at once — see
      # https://github.com/expo/expo/issues/50067 and the (closed, "fixed
      # by a newer Xcode instead") PR https://github.com/expo/expo/pull/49740.
=======
>>>>>>> 444a71f76128b00845ecf2152bb5faa4d8272fe2
      - name: Select Xcode 26.6 (Swift 6.3 — satisfies both expo-modules-jsi constraints)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.6"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install JS dependencies
        run: npm install

      - name: Generate the native iOS project (expo prebuild)
        run: npx expo prebuild --platform ios --clean --no-install

<<<<<<< HEAD
      # RCT_USE_PREBUILT_RNCORE=0: React Native 0.84+ defaults to pulling a
      # precompiled ReactNativeDependencies.xcframework via a separate large
      # Swift Package. Building from source instead avoids that package
      # entirely, which kept an earlier build attempt from hitting a second,
      # unrelated Swift-tools-version conflict on top of the one above.
=======
>>>>>>> 444a71f76128b00845ecf2152bb5faa4d8272fe2
      - name: Install CocoaPods dependencies (build React Native from source)
        working-directory: ios
        env:
          RCT_USE_PREBUILT_RNCORE: "0"
        run: pod install

      - name: Find the generated workspace + scheme
        id: xcode
        working-directory: ios
        run: |
          WORKSPACE=$(ls -d *.xcworkspace | head -n 1)
          SCHEME=$(basename "$WORKSPACE" .xcworkspace)
          echo "workspace=$WORKSPACE" >> "$GITHUB_OUTPUT"
          echo "scheme=$SCHEME" >> "$GITHUB_OUTPUT"
          echo "Found workspace: $WORKSPACE, scheme: $SCHEME"

      - name: Build for a real device, unsigned
        working-directory: ios
        run: |
          xcodebuild build \
            -workspace "${{ steps.xcode.outputs.workspace }}" \
            -scheme "${{ steps.xcode.outputs.scheme }}" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            ENABLE_USER_SCRIPT_SANDBOXING=NO

      - name: Package the .app into an unsigned .ipa
        working-directory: ios
        run: |
          APP_PATH=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" | head -n 1)
          echo "Found app bundle: $APP_PATH"
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -r -y ArcIsland-unsigned.ipa Payload
          mkdir -p ../ipa-output
          mv ArcIsland-unsigned.ipa ../ipa-output/

      - name: Upload the .ipa as a downloadable build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ArcIsland-unsigned-ipa
          path: ipa-output/ArcIsland-unsigned.ipa
          retention-days: 30
